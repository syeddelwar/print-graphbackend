'use strict';

/**
 * service-page-slider router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::service-page-slider.service-page-slider');
