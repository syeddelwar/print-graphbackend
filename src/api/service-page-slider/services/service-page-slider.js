'use strict';

/**
 * service-page-slider service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::service-page-slider.service-page-slider');
